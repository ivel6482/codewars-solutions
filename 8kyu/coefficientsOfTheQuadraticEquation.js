function quadratic(x1, x2) {
	// Answer in array
	let a = 1
	let b = -x1 + -x2
	let c = x1 * x2
	return [a, b, c]
}

// Reference:
// https://www.onlinemath4all.com/formation-of-quadratic-equation-with-given-roots.html
